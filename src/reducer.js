import { combineReducers } from 'redux'

const Application = ((state = {}, { type, payload, ...action }) => {
    switch(type) {

        default:
            return state
    }
})

export default combineReducers({
    Application
})